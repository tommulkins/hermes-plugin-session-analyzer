{
  "name": "hermes-plugin-session-analyzer",
  "version": "0.2.1",
  "private": true,
  "description": "Session Analyzer — Hermes desktop + backend plugin",
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "node scripts/lint-plugin.mjs",
    "lint:strict": "node scripts/lint-plugin.mjs --strict",
    "lint:test": "node --test scripts/lint-plugin.test.mjs"
  },
  "dependencies": {
    "prettier": "^3.9.6"
  }
}
